# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: NETFLIX/netflix-gpt   # 👈 ensures we build from subfolder
    steps:
      - uses: actions/checkout@v4
      - run: npm ci                             # install dependencies
      - run: npm run build                      # build your app
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_NETFLIX_GPT_5D882 }}
          channelId: live
          projectId: netflix-gpt-5d882
